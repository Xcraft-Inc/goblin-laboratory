{
  "create": {
    "parallel": true,
    "desc": "Create a widget",
    "options": {
      "params": {
        "optional": "config..."
      }
    }
  },
  "open": {
    "parallel": true,
    "desc": "Open a widget",
    "options": {
      "params": {
        "required": "route"
      }
    }
  }
}